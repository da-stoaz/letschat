# Pick one production template, then copy to `.env`:
#
#   cp .env.production.tunnel.example .env
#   # or
#   cp .env.production.caddy.example .env
#
# This file is intentionally minimal to avoid mixing track-specific variables.
