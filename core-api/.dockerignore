# Used by core-api/Dockerfile, which COPYs only `global.json` and `src/`.
# `tools/` was kept in the context while the migrator had its own image built
# from tools/CoreApi.Migrator/Dockerfile; that image is gone, so it's excluded
# again.
tools/
**/bin/
**/obj/
*.md
.env*
