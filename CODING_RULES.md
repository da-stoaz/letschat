

- There is no such thing as backwards compatibility of a user facing graphical interface. Backwards comaptibility can only ever be done to data, endpoints and reducers. Not to .tsx component names. 